@using PikaShop.Common.Pagination
@model PaginatedList<PikaShop.Admin.ViewModels.DepartmentViewModel>

@{
    ViewData["Title"] = "Index";
}

<h4 class="page-title">Departments</h4>

<p>
    <a asp-action="Create" class="btn btn-primary">Create New</a>
</p>

<table class="my-table mt-3">
    <thead class="custom-class bg-white">
        <tr>
            <th>
                ID
            </th>
            <th>
                Name
            </th>
            <th>
                Description
            </th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr onclick="window.location.href = '@Url.Action("Details", new { id = item.ID })';">
                <td>
                    @Html.DisplayFor(modelItem => item.ID)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td>
                    <div class="form-button-action">
                        <button type="button" data-toggle="tooltip" title="Edit" class="btn btn-link <btn-simple-primary">
                            @Html.ActionLink("", "Edit", new { id = item.ID }, new { @class = "la la-edit" })
                        </button>
                        <button type="button" data-toggle="tooltip" title="Remove" class="btn btn-link btn-simple-danger">
                            @Html.ActionLink("", "Delete", new { id = item.ID }, new { @class = "la la-times btn-simple-danger" })
                        </button>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>
@{
    var prevDisabled = !Model.HasPrevious ? "disabled" : "";
    var nextDisabled = !Model.HasNext ? "disabled" : "";
}
@*
    // Display Previous
    <a asp-action="Index"
       asp-route-pageNumber="@(Model.CurrentPage - 1)"
       class="btn btn-default @prevDisabled">
        Previous
    </a>
    for(i 1 -> Model.TotalPages) {
        if(i == Model.CurrentPage) {
            // Display as active (selected)
            <a asp-action="Index"
               asp-route-pageNumber="@i"
               class="btn btn-default active">
                i
            </a>
        }
        else {
            // Display as regular
            <a asp-action="Index"
               asp-route-pageNumber="@i"
               class="btn btn-default">
                i
            </a>
        }
    }
    // Display Next
    <a asp-action="Index"
       asp-route-pageNumber="@(Model.CurrentPage + 1)"
       class="btn btn-default @nextDisabled">
        Next
    </a>
*@
<a asp-action="Index"
   asp-route-pageNumber="@(Model.CurrentPage - 1)"
   class="btn btn-default @prevDisabled">
    Previous
</a>
<a asp-action="Index"
   asp-route-pageNumber="@(Model.CurrentPage + 1)"
   class="btn btn-default @nextDisabled">
    Next
</a>